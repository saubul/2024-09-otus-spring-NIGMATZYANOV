spring:
  datasource:
    url: jdbc:postgresql://localhost:5450/db
    username: postgres
    password: postgres
  sql:
    init:
      mode: never
      schema-locations: classpath:schema.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true